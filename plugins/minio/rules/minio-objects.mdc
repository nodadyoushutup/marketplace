---
description: >-
  List buckets/objects, read metadata, and upload/download via MCP after resolving names.
alwaysApply: false
---

# Minio Objects

List buckets/objects, read metadata, and upload/download via MCP after resolving names.

Prefer the connected MCP for this product (`global-mcp-first`). Never invent
ids — list/resolve first.
