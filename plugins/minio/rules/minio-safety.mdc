---
description: >-
  Delete bucket/object and public ACL changes need an explicit verb. Never paste access keys into chat.
alwaysApply: false
---

# Minio Safety

Delete bucket/object and public ACL changes need an explicit verb. Never paste access keys into chat.

Prefer the connected MCP for this product (`global-mcp-first`). Never invent
ids — list/resolve first.
