---
description: >-
  Search logs via MCP with explicit query + time range. Prefer streams/fields discovery first.
alwaysApply: false
---

# Graylog Search

Search logs via MCP with explicit query + time range. Prefer streams/fields discovery first.

Prefer the connected MCP for this product (`global-mcp-first`). Never invent
ids — list/resolve first.
