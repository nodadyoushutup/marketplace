---
description: >-
  List nodes, VMs, storage, and pools via MCP. Never invent node names or VMIDs — list/resolve first.
alwaysApply: false
---

# Proxmox Inventory

List nodes, VMs, storage, and pools via MCP. Never invent node names or VMIDs — list/resolve first.

Prefer the connected MCP for this product (`global-mcp-first`). Never invent
ids — list/resolve first.
