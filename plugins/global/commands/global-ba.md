---
name: global-ba
description: >-
  Shape problem, outcomes, requirements, and acceptance criteria before build.
---

# Business analysis

1. Launch `global-business-analyst` (read-only) with the user ask.
2. Deliver problem, outcome, requirements, acceptance criteria, out of scope, assumptions.
3. Hand off to `global-plan` / `global-planner` when architecture is still ambiguous, otherwise to implementation via `code-workflow` if the `code` plugin is installed.
