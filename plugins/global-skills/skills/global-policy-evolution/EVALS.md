# Global Policy Evolution Evaluations

Verify Cursor rule/skill/agent validation, forbidden names, `--changed`,
`--json`, secrets, links, and symlinks. Tests use temporary
`.cursor/policy-config.json` fixtures and never depend on the host repository
overlay.
