---
description: >-
  Sync / refresh / resource actions need an explicit verb naming the app. Prefer dry-run or status read first.
alwaysApply: false
---

# Argocd Sync

Sync / refresh / resource actions need an explicit verb naming the app. Prefer dry-run or status read first.

Prefer the connected MCP for this product (`global-mcp-first`). Never invent
ids — list/resolve first.
