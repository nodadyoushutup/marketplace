---
description: >-
  List/search library and download state via yarr MCP tools. Resolve instance/service names — never invent.
alwaysApply: false
---

# Yarr Inventory

List/search library and download state via yarr MCP tools. Resolve instance/service names — never invent.

Prefer the connected MCP for this product (`global-mcp-first`). Never invent
ids — list/resolve first.
