---
name: prometheus
description: >-
  Prometheus discover or query.
---

# prometheus

1. Load `prometheus`. Prefer a ready MCP (`global-mcp-first`).
2. Follow the skill gate and load map.
3. Never invent ids; report evidence (names, status, links — no secrets).
