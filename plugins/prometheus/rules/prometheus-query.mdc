---
description: >-
  Instant and range queries: prefer rate-then-sum; keep cardinality bounded; never invent metric names.
alwaysApply: false
---

# Prometheus Query

Instant and range queries: prefer rate-then-sum; keep cardinality bounded; never invent metric names.

Prefer the connected MCP for this product (`global-mcp-first`). Never invent
ids — list/resolve first.
