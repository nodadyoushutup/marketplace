---
name: browser-qa
description: >-
  Browser QA via Playwright MCP, IDE browser fallback, then agent-browser CLI.
---

# Browser QA

1. Load `browser-automation`.
2. Prefer a ready Playwright MCP, else IDE browser MCP, else `agent-browser` CLI.
3. Report decisive evidence (URL, assertion, screenshot path if taken).
