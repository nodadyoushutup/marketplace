---
name: browser-qa
description: >-
  Browser QA via Cursor IDE browser MCP, then agent-browser CLI fallback.
---

# Browser QA

1. Load `browser-automation`.
2. Prefer a ready IDE browser MCP (`cursor-ide-browser`), else `agent-browser`
   CLI.
3. Report decisive evidence (URL, assertion, screenshot path if taken).
